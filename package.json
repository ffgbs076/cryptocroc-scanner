{
  "name": "cryptocroc-forest-site",
  "private": true,
  "version": "1.0.0",
  "description": "Forest line (truth + live preview) + probability gate + structure confluence",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "echo \"no build\""
  }
}